api:
  dashboard: true
  insecure: true
  debug: true

entryPoints:
  web:
    address: ":80"
  traefik:
    address: ":8080"

providers:
  docker:
    exposedByDefault: false
    network: akschim-network

log:
  level: INFO
  format: json
